spring:
  config:
    activate:
      on-profile: dev
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: true
logging:
  level:
    org:
      hibernate:
        type: trace

